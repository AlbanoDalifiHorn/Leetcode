/*
    Approach: Greedy, so we always remove the ballon with minimum time
    Time complexity: O(n) and space complexity: O(1)
    Idea:
        1. Iterate through array and check if current color equals previous color
        2. If yes: add the minimum time to result (we remove the cheaper balloon)
        3. Update neededTime[i] to the maximum of both values
        4. This ensures for consecutive duplicates, we always keep the most expensive
           and remove all cheaper ones
*/
class Solution {
    public int minCost(String colors, int[] neededTime) {
        int res = 0;

        for (int i = 1; i < neededTime.length; i++) {
            if (colors.charAt(i) == colors.charAt(i - 1)) {
                res += Math.min(neededTime[i], neededTime[i - 1]);
                neededTime[i] = Math.max(neededTime[i], neededTime[i - 1]);
            }
        }
        return res;
    }
}
